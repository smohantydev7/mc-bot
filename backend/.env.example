# === AI Provider (pick one) ===

# Option 1: Ollama (FREE, runs locally on your laptop)
# Just install Ollama and pull a model — no key needed
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# Option 2: Anthropic Claude API (paid, needs API key)
# ANTHROPIC_API_KEY=sk-ant-xxxxx
